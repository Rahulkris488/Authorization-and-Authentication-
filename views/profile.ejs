<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class=" w-full h-screen bg-zinc-900 text-white p-10 font-">
     <div class="w-full flex justify-end">   
        <a class="bg-red-500 rounded-md px-3 text-sm mb-5 inline-block  py-2" href="/logout">Log out</a>
    </div>
    <h3 class="text-3xl mb-3"><span class="font-light">Hello</span> <%= user.username%> :) </h3>
    <h5 class="mb-5 font-mono">You can create a new post</h5>
    <form method="post" action="/post">
        <textarea placeholder="What's on your mind " class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""></textarea>
        <input class="px-3 py-2 bg-blue-500 block rounded-md mt-3 w-44" type="submit" value="Create new post" name="" id="">
    </form>
    <div class="posts mt-10">
        <h3 class="text-zinc-400" >Your Posts.</h3>
    <div class="postcontainer mt-2">
  <% user.posts.reverse().forEach(function(posts){ %>
      
    <div class="post w-1/3 mb-3 p-4 border-3 border-zinc-800 mt-2">
        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
        <p class="text-sm tracking-tight"> <%= posts.content %> </p>
        <div class="btns flex gap-4 mt-5">
            <a class="text-blue-500">Like</a>
            <a class="text-zinc-500">Edit</a>
        </div>
    </div>

  <%})%>
    </div>
    </div>
    </div>
</body>
</html>      