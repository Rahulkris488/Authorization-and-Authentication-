<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class=" w-full min-h-screen bg-zinc-900 text-white p-10 font-">
     <div class="w-full flex justify-end">   
        <a class="bg-red-500 rounded-md px-3 text-sm mb-5 inline-block  py-2" href="/logout">Log out</a>
    </div>
    <h5 class="mb-5 font-mono">Edit your post</h5>
    <form method="post" action="/update/<%= post._id%>">
        <textarea placeholder="What's on your mind " class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""><%= post.content%></textarea>
        <input class="px-3 py-2 bg-yellow-500 block rounded-md mt-3 w-44" type="submit" value="Update post" name="" id="">
    </form>
    </div>
</body>
</html>      